# ── Domínio público
DOMAIN=mm.seudominio.com

# ── Database 
DATABASE_URL=postgresql://mm_app:changeme@postgres:5432/money_manager

# ── API 
# openssl rand -base64 32
JWT_ACCESS_SECRET=
# openssl rand -base64 32
JWT_REFRESH_SECRET=
# openssl rand -base64 32
INTERNAL_API_KEY=
CORS_ORIGINS=https://mm.seudominio.com
RUN_DB_MIGRATIONS=true

# ── Web 
VITE_API_URL=https://mm.seudominio.com

# ── Bot
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=BotSemArroba
# openssl rand -hex 16
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_USE_POLLING=false

# ── STT 
STT_MODEL=small
STT_LANGUAGE=pt
STT_NOISE_REDUCTION=false
